# Admin Backend URL (admin-backend project)
PLATFORM_API_URL=http://localhost:3000

# Same JWT secret as admin-backend (so platform-issued tokens are valid)
JWT_SECRET=your-jwt-secret

# Optional: server-to-server token. If set, requests without Authorization use this.
# PLATFORM_SERVICE_TOKEN=

# Customer backend port
PORT=3001

# Allowed CORS origin (e.g. customer app or portal)
# CORS_ORIGIN=http://localhost:5174
