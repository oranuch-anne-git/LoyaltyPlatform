# PostgreSQL (required). Local: postgresql://USER:PASSWORD@localhost:5432/DBNAME. Render: use Internal Database URL from your Postgres service.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/loyalty"
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
PORT=3000
# Optional: API key (16+ chars) for JWT via POST /api/auth/token (X-API-Key header or body apiKey). Comma-separated for multiple keys.
# API_KEY="Y1768IUTGX4K2M9PQ"
LINE_CHANNEL_SECRET=""
LINE_CHANNEL_ACCESS_TOKEN=""
CORS_ORIGIN="http://localhost:5173"
