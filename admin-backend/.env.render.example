# Render External Database URL (one line). Copy this file to .env.render and paste your URL.
# From Render: Dashboard → your Postgres service → Connect → External Database URL
# Add ?sslmode=require if not present.
postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
